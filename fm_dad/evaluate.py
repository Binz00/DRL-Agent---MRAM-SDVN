"""
evaluate.py — Evaluation and verification for trained FM-DAD agents.

STAGE 3 — This file is a STUB in Stage 1. It will be fully implemented in Stage 3.

Verification tasks (Section 10, Steps 11–12):
    (a) Moving-average reward plot per agent → models/<agent>_reward.png
    (b) Policy check: clear attackers → should pick a3/a4;
                      clear innocents → should pick a0;
                      upstream victims → should pick a0/a1.
    (c) Final summary table: agent | avg_reward | attacker_acc | innocent_acc

This file is intentionally left as a stub during Stage 1.
Do NOT run or modify until Stage 3 is approved.
"""

# ---------------------------------------------------------------------------
# STUB — Stage 3 implementation placeholder
# ---------------------------------------------------------------------------

raise NotImplementedError(
    "evaluate.py is a Stage 3 module. "
    "Do not run it during Stage 1 or Stage 2. "
    "It will be implemented after Stages 1 and 2 are approved."
)
